<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="path/to/font-awesome/css/font-awesome.min.css">
  <link rel="stylesheet" href="style.css">
  <title>To Do App</title>

</head>
<body>
  <h1>To Do List</h1>
  <ul class="list">
    <% for(let i=0; i< info.length; i++) {%>
        <li class="item">
          <span><%= info[i].itemName %></span>
          <span><%= info[i].itemTime %></span>
          <span><%= info[i].check %></span>
          <span class="fa fa-check-circle"></span>
          <span class="fa fa-trash"></span>
        </li>  
    <% } %>
  </ul>

  <h2>Add a new item to do</h2>

  <form action="/addItem" method="POST">
    <input type="text" placeholder="Item to do" name="itemName">
    <input type="text" placeholder="Time to get done" name="itemTime">
    <input type="submit">
  </form>

  <script src="/public/main.js"></script>
</body>
</html>